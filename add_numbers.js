function calculate_form() {

    let first_num = Number(document.getElementById('first_num').value);
    let second_num = Number(document.getElementById('second_num').value);

    let total = first_num + second_num;

    alert(total);
}